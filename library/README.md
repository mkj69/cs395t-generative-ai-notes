# Source library

This is an evidence index, not a storage folder for downloaded PDFs.

- `references.bib` is the canonical citation database.
- `papers.yml` stores public-facing metadata and narrow claim records.
- `reading-notes/` stores source-specific notes only after the source is read.

Every record should distinguish what the source directly establishes from the
author's interpretation.
