# 不依赖 AI 的手工笔记工作流

这份手册规定如何从一份课程讲义出发，亲手建立学习笔记，并在真正产生
问题之后再建立研究笔记。AI 不参与理解、概括、推导、措辞或检查答案。

## 先区分两种笔记

### Learning note

- 按课程顺序组织，可以直接使用 lecture 编号。
- 目标是确认自己能否重建定义、算法、公式、证明和例子。
- 允许保留错误、空白和尚未理解的步骤。
- 不要求产生新的研究观点。

### Research note

- 按问题组织，不按 lecture 编号组织。
- 从已经完成的学习笔记中分叉出来。
- 需要自己的问题、论证、跨论文连接或可检验的假设。
- 使用 `notes/_template/`，而不是学习笔记模板。

## 五条手工原则

1. **先理解，再成文。** 第一次阅读时只记关键词、符号和问题，不写流畅总结。
2. **关掉材料后重建。** 真正的正文首先来自记忆和自己的推导，而不是边看边改写。
3. **保留错误轨迹。** 写下失败的推导、误解及修正原因，不把学习过程修饰得过于顺利。
4. **来源与自己的话分开。** 讲义中的定义和定理要标注来源；自己的解释要明确标注为重建。
5. **不知道就留空。** 使用 `[UNCLEAR]`，不要用顺畅但未经验证的文字填补理解缺口。

## 建议使用的标记

在草稿中使用这些短标签：

- `[SOURCE]`：从课程材料中逐字记录的符号、定理名称或必要定义。
- `[RECALL]`：合上材料后，凭记忆写出的解释。
- `[DERIVATION]`：自己逐步完成的推导。
- `[ALGORITHM]`：用自己的顺序重写算法，并标出输入、输出和停止条件。
- `[EXAMPLE]`：自己构造的例子或反例。
- `[UNCLEAR]`：尚未理解，不能继续假装推导。
- `[CORRECTION]`：核对材料后发现的错误及其原因。
- `[QUESTION]`：下一次学习要解决的问题。
- `[IDEA]`：可能发展成研究笔记的想法；先停放，不在学习笔记中展开。

## 一次完整学习流程

### 0. 建立空白笔记

1. 复制 `course-notes/_template/`。
2. 将目录命名为 `lecture-XX-short-topic`。
3. 将 `learning-note-template.md` 改名为
   `lecture-XX-short-topic.md`；Logseq 中每个 Markdown 文件名必须唯一。
4. 填写标题、日期、讲义链接和状态。
5. 将状态设为 `capturing`。
6. 在开始学习前，不填写任何解释性正文。

### 1. 第一遍：建立地图（材料打开）

目标不是理解所有细节，而是找出结构。

- 写下章节标题和依赖关系。
- 圈出新符号、假设、算法和定理。
- 每遇到一个不明白的跳步，立刻记为 `[QUESTION]`。
- 不复制完整段落。
- 不尝试把笔记写得漂亮。

完成标准：能够在一页纸上画出这节课的结构，但还不需要会证明。

### 2. 第二遍：闭卷重建（材料关闭）

对每个主要概念执行同一套动作：

1. 用一句自己的话说明它解决什么问题。
2. 写出所需假设和符号。
3. 从第一步开始重推公式或算法。
4. 构造一个最小数值例子。
5. 写出它不能说明什么。
6. 卡住时写 `[UNCLEAR]` 并停止，不查看答案填空。

完成后再打开讲义核对。不要直接覆盖错误；在错误下面添加
`[CORRECTION]`，解释错在哪里。

### 3. 第三遍：证明与计算检查

- 每个等号旁写明理由。
- 每个概率或期望写明随机变量和条件。
- 每个定理列出适用模型、精度、独立性及分布假设。
- 至少做一个手算例子。
- 如果写代码，先在纸上写出期望结果，再运行程序。
- 结果与预期不同，先检查自己的理解，不修改预期来迎合输出。

### 4. 第四遍：压缩成可读笔记

只有在完成闭卷重建后才整理语言。

- 删除纯粹重复讲义目录的段落。
- 保留自己的例子、推导、错误和修正。
- 把长段落改成问题、结论和证据之间的结构。
- 将仍未解决的内容集中到 `What is still unclear?`。
- 将研究灵感移动到 `Research parking lot`，不在此处提前论证。

### 5. 最终核对

逐项检查：

- [ ] 我能在不看材料时讲清本节的主线。
- [ ] 所有符号都在使用前定义。
- [ ] 所有关键公式至少重推过一次。
- [ ] 我知道每个定理依赖哪些假设。
- [ ] 我至少写了一个自己的例子。
- [ ] 未解决的问题没有被隐藏。
- [ ] 讲义和原论文链接已经记录。
- [ ] 没有把下载的 PDF 放进仓库。
- [ ] 正文没有由 AI 生成、改写或补全。

通过后，将状态从 `capturing` 改为 `reconstructing`；只有完成来源核对后，
才能改为 `checked`。

## 90 分钟手工学习节奏

可以在一次学习中使用以下节奏：

| 时间 | 动作 | 产物 |
| --- | --- | --- |
| 0–15 分钟 | 快速阅读和标记结构 | 章节地图与问题列表 |
| 15–40 分钟 | 关闭材料，重建概念 | `[RECALL]` 段落 |
| 40–65 分钟 | 手推公式或算法 | `[DERIVATION]` |
| 65–75 分钟 | 自己构造例子 | `[EXAMPLE]` |
| 75–85 分钟 | 打开材料核对 | `[CORRECTION]` |
| 85–90 分钟 | 写下下一步 | `[QUESTION]` 与 `[IDEA]` |

## 什么时候建立研究笔记

只有在下面至少一项成立时才新建研究笔记：

- 两篇以上资料给出了不一致的解释或假设。
- 一个定理的边界条件引出了明确问题。
- 一个失败的推导暴露了值得研究的结构。
- 一个想法能够写成可证伪的假设。
- 已经能够描述最小实验、证明或反例。

研究笔记应链接回产生它的 learning note；learning note 只保留一条简短的
`[IDEA]` 记录。

## 发布规则

- `capturing` 可以公开，但必须清楚显示为进行中，不能让空白模板看起来像完成内容。
- `reconstructing` 表示已有自己的推导，但仍可能包含错误。
- `checked` 表示已回到讲义及实际阅读过的原始来源核对。
- 发布学习笔记不等于声称课程内容是原创；原创的是重建过程、例子和理解记录。
- Research note template 始终保留，供后续问题型写作使用。
